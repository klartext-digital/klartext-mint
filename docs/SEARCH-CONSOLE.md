# Search Console: klartext-digital.ch

Stand: 16. September 2026.

## Erledigt

- Domain-Property `sc-domain:klartext-digital.ch` eingerichtet.
- Zusätzlicher TXT-Datensatz bei Hostinger nach ausdrücklicher Freigabe gespeichert: Name `@`, TTL `300`.
- Nachweis beim autoritativen Nameserver öffentlich sichtbar.
- Google meldet **Inhaberschaft bestätigt**, Bestätigungsmethode Domainnamen-Anbieter.
- Bestehende DNS-Einträge für Website und E-Mail wurden nicht verändert.

Der bestätigte DNS-Eintrag muss erhalten bleiben. Der kontobezogene Wert und private Kontaktdaten werden nicht im öffentlichen Repository dokumentiert. Für diese Domain-Verifikation ist kein Meta-Tag auf der Website erforderlich; `verification.google` in der Website-Konfiguration bleibt deshalb leer.

## Noch nicht erledigt

Die Domain liefert weiterhin die Hostinger-Parkseite. Die Website ist als Vorschau auf GitHub Pages veröffentlicht und weiterhin `noindex`. Eine erfolgreiche Inhaberbestätigung bedeutet noch keine Indexierung der Website.

Nach freigegebenem Produktivstart:

1. Den finalen Build auf `https://klartext-digital.ch/` ausliefern; bevorzugten Host, HTTPS, Weiterleitungen und echte 404 prüfen.
2. Canonicals und interne Links auf die finale Domain prüfen. Bestätigte Inhalte und Betreiberangaben freigeben.
3. Nur freigegebene Seiten indexieren und eine nicht leere Sitemap mit finalen URLs erzeugen.
4. `https://klartext-digital.ch/sitemap.xml` in dieser Domain-Property einreichen.
5. Homepage, Abo, Webdesign und Wissensartikel mit der URL-Prüfung testen; Freigaben und Indexierung kontrollieren.
6. Später Suchanfragen, Impressionen, Klicks und tatsächliche Anfragequalität zusammen auswerten.

Keine GitHub-Vorschau-Sitemap in der Domain-Property einreichen. Bing wurde in diesem Arbeitsblock nicht eingerichtet.
